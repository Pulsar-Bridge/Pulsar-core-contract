# Pulsar-core-contract
Soroban smart contract powering Pulsar Bridge — the on-chain source of truth for deposit transactions, tracking their lifecycle from Pending through Completed with a locked, versioned event schema for downstream consumers.
